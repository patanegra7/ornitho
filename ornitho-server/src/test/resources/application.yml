spring:
    application:
        name: Ornitho-server-test
    data:
        mongodb:
            host: localhost
            port: 27017
            database: ornithodb-test
            
logging:
    level:
        cat.dme.ornitho: TRACE
